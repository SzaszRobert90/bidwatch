[Unit]
Description=bidwatch pull-deploy (main -> live stack)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=deploy
WorkingDirectory=/opt/bidwatch
Environment=HOME=/home/deploy
ExecStart=/opt/bidwatch/infra/deploy.sh
